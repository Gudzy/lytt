# yt-dlp config for lytt container.
# Webshare residential proxy is passed via --proxy at runtime (WEBSHARE_PROXY_URL env var).
# No GetPOT/bgutil — residential proxy bypasses YouTube bot detection without PO tokens.
